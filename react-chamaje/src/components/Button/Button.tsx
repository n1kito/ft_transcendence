import React from 'react';
import './Button.css';

// TODO: the border around the button should be around the shadow as well, like on gandi

const Button = () => {
	return (
		<div>
			<div id="buttonWrapper">
				<button id="coloredButton">ok.</button>
			</div>
		</div>
	);
};

export default Button;
